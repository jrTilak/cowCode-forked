# Copy to .env and fill in your keys. .env is gitignored.
# State dir: default ~/.cowcode (override with COWCODE_STATE_DIR).

# LLM API keys (setup prompts for these; vision fallback uses same key when same provider)
LLM_1_API_KEY=
LLM_2_API_KEY=
LLM_3_API_KEY=

# Optional: Brave Search API for web search
# BRAVE_API_KEY=

# Speech (voice): set during setup. Whisper = voice-to-text (or use LLM_1_API_KEY). 11Labs = text-to-voice.
# SPEECH_WHISPER_API_KEY=
# ELEVEN_LABS_API_KEY=

# Optional: Telegram bot token (from @BotFather)
# TELEGRAM_BOT_TOKEN=
